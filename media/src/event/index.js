/**
 * Maps command/event keys to their corresponding service method handlers.
 * Replace `null` with actual function references from the service as needed.
 */
export default {
    GET_PROFILE: null // To be replaced with: customerService.getProfile.bind(customerService)
};
